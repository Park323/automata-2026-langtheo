# 과제 1 — 구현·검증 결과

> `feat/01-config-and-loop` · カキガワ言語研究班
> 검증 환경: Python 3.11.9 / pytest 9.1.1 / pyyaml 6.0.3 (Windows)
> 실행: `py -3.11 -m pytest -q` → **18 passed**

---

## 1. 합격 기준 10개 — 실제 실행 결과

숫자는 실제 실행 출력이다(추정 아님).

| # | 검사 | 기대 | 실제 | |
|---|---|---|---|---|
| 1 | 인구 불변 | 항상 9명 | 항상 9명 | ✅ |
| 2 | 런당 사망 수 | 46~55 | 49.2 (procreate off, 30시드 평균) | ✅ |
| 3 | 수명 분포 | 평균 8.2~8.4, 최대 ≤12 | 8.20, 최대 11 | ✅ |
| 4 | 나이 10↑ 생존 | ~1% | survival(10)=0.0099 | ✅ |
| 5 | 재현성 | 같은 seed 로그 바이트 동일 | 동일 | ✅ |
| 6 | 예산 보존 | budget<0 없음 | 없음 | ✅ |
| 7 | 신규 첫 행동 | 태어난 턴 행동 없음 | 없음(uid 기준) | ✅ |
| 8 | procreate | 자식=부모 잔여 / 자연사=0 | 자연사 전부 0, procreate 37건 발생 | ✅ |
| 9 | 국토 배타성 | 투표 없이 안 바뀜 | 안 바뀜 | ✅ |
| 10 | assert | 확정 config 통과 + 변형 검출 | 아래 3장 | ✅ |

**survival 단위(B-1) 정합성:**
`expected_life=8.2788` (기대 8.28), `survival(10)=0.0099`,
`hazard 0..9 = [0, 0, 0, 0, 0.01, 0.06, 0.17, 0.40, 0.70, 0.93]` — 명세와 완전 일치.

---

## 2. window 값 (A-4 자가검증)

```
A=2700   B=4500   E=6480   <   interceptor=8019   <   C×0.6=8100
벙커 하한=900   bunker_scale=2700   벙커 상한=4500
벙커 1인부담=90.0  >  요격기 1인부담=17.8
기본 config check_all 실패 수 = 0
```

`facility.eff`는 명세에 `?`로 비어 있어 A-4 목표에서 역산: `A = 9000 × eff × 0.3 = 2700 → eff = 1.0`.

---

## 3. 8개 assert — 트리거 검증 (어떤 값으로 걸리는지)

각 검사를 일부러 걸리게 하는 값을 넣어 **실제로 걸리는지** 확인했다.

| 넣은 값 | 걸린 검사 | 비고 |
|---|---|---|
| 기본 (변경 없음) | (없음) | 정상 통과 |
| `interceptor = 2500` | ★A, ★B, ★E | 하한 전부 아래 |
| `interceptor = 4000` | ★B, ★E | A 위, B·E 아래 |
| `interceptor = 5000` | **★E 단독** | A·B 위, E 아래 |
| `interceptor = 6480` | ★E | 경계값(`>` 엄격 → 걸림) |
| `interceptor = 8100` | ★C | 경계값(`<` 엄격 → 걸림) |
| `interceptor = 8200` | **★C 단독** | 상한 초과 |
| `bunker_scale = 900` | (없음) | 하한 경계(`>=` 포함 → 통과) |
| `bunker_scale = 800` | **벙커↓ 단독** | 하한 미만 |
| `bunker_scale = 500` | 벙커↓, 부담 | 매우 얕음 |
| `bunker_scale = 4500` | (없음) | 상한 경계(`<=` 포함 → 통과) |
| `bunker_scale = 5000` | **벙커↑ 단독** | 상한 초과 |
| `comm_intl_ai = [6,12,24,48]` | (없음) | 기본, 정상 |
| `comm_intl_ai = [5,12]` | 노브 | 5는 learner(5) 초과 아님 |
| `comm_intl_ai = [4,12]` | 노브 | 최저값이 learner 이하 |
| `success_prob = 0.5` | ★E | 창이 위로 이동 |
| `success_prob = 0.2` | ★C | 창이 아래로 축소 |

**관측된 구조:**
- **하한 검사는 중첩된다**(★A ⊂ ★B ⊂ ★E). ★A는 가장 느슨해 **단독으로는 걸 수 없다**.
  ★E만 단독으로 걸 수 있다(`interceptor=5000`).
- **경계는 하한이 엄격(`>`), 벙커는 포함(`>=`,`<=`)** 으로 동작한다.
- **부담 검사는 단독 격리가 불가능하다.** 부담이 걸리려면 `bunker ≤ interceptor/15 ≤ 540`
  인데, 그러면 벙커↓(하한 900)가 먼저 걸린다. 기본 월드 크기의 구조적 성질.

---

## 4. 50턴 1회 실행 요약 (제출물)

더미 정책(예산 절반을 자국 요격기에 투자, 7살에 procreate)으로 seed 1 실행:

> 50턴 완료. 사망(=출생) 54회(자연사 17 / procreate 37). 3개국 모두 국토를 요격기로
> 확정, 진척 합계 12,708 > 임계 8,019 → **전 인류 생존(all_survive)**. seed 7 도 55회
> 사망·요격기 12,838으로 동일 결론. 더미가 무조건 요격기에 투자하므로 조율 실패·배신은
> 일어나지 않으며, 결과는 seed와 무관하게 항상 생존이다. 이는 정상이다 — 과제 1의 목표는
> 창발 관측이 아니라 **세계가 결정론적으로 정확히 도는가**의 확인이고, 의미 있는 실험은
> 더미를 LLM으로 교체하는 과제 2에서 시작된다.

---

## 5. 문서(A-4)와 스펙 본문이 어긋나는 지점 — 확인 필요

구현은 **스펙 7장 공식을 그대로** 따랐다. 아래는 과제 문서 A-4 표가 스펙 공식과 불일치하는 부분이다.

| # | A-4 표 주장 | 스펙 공식상 실제 | 원인 |
|---|---|---|---|
| ① | `success_prob=0.5` → ★C | **★E** 가 걸린다 | success_prob↑ 이면 창이 위로 이동해 interceptor가 하한(E) 아래로. ★C(상한)는 오히려 여유로워짐. ★C는 success_prob를 **낮출 때**(0.2) 나온다 |
| ② | `bunker_scale=1000` → 벙커↓ | **안 걸린다** | 스펙 하한이 `to_progress(국가_한주기)=900`. 1000≥900이라 통과. 벙커↓ 시연은 900 미만(800)이어야 |
| ③ | 더미 procreate(age≥7) + #3 수명 8.2~8.4 | procreate 켜면 수명 **6.91** (7살에 강제 사망) | 더미 명세와 캘리브레이션 목표가 동시 충족 불가. #2(사망수)는 procreate 켜도 54.4로 통과하나 #3(수명)은 실패 |

**대응:** ①② 는 값·라벨을 스펙에 맞게 이해하고 테스트에 명시적으로 고정(주석 포함).
③ 은 수명 모델 캘리브레이션(#2,#3)을 `procreate_age=None`로 격리 측정하고, 상속 검증(#8)은
procreate를 켜고 따로 확인하도록 분리했다.

---

## 6. 명세에 없어 임의로 정한 것

| 항목 | 결정 | 근거 |
|---|---|---|
| `facility.eff` | 1.0 | A-4 목표(A=2700)에서 유일하게 역산되는 값 |
| 국토 최초 확정 용도 | `interceptor` (`loop.py`의 `DEFAULT_FACILITY_TYPE`) | 더미는 용도를 정하지 않음. 과제 1엔 투표·LLM이 없어 기본값 고정. 과제 2에서 에이전트가 정함 |
