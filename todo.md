# TODO

```
팀명   カキガワ言語研究班
리포   github.com/Park323/automata-2026-langtheo
마감   8/30 23:59   ·   남은 15일 (8/15 기준)
```

> 팀명은 `README.md` 최상단, 발표자료 표지, 제출 폼에 동일하게.

## 끝난 것

- [x] 참가 등록 + Discord · 파일럿 문장 10개 · 화용 표지 사전 v0
- [x] **번역 파일럿** (8/12) — 324콜. 모델 `mistral-small-3.2-24b` 확정,
      길이비 `fr:ja:zh = 100:33:23` 실측, 지표 6a 정의 수정, 숫자 0/34 소실
- [x] **Phase 0 사전 밸런스** (8/12~13) — 설계 결함 8건을 세계 만들기 전에 적발.
      판정 기준을 회피율 → `w*` 로 교체
- [x] **세계 구조 전환** (8/13) — 세대 경계 폐지, 확률적 수명, 행복→수명,
      `procreate`, 학습 할인, 파라미터 전량 확정
- [x] **과제 1** (PR #4) — config 로더 + assert 8개 + 턴 루프 7단계 (LLM 없이)
- [x] **과제 2** (PR #6) — function calling agentic loop · 메시지 · 번역 · 3턴 스모크
- [x] 프롬프트 3언어화 + 언어 위생 (8/15) — 에이전트가 영어로 쓰던 문제

## 끝난 것 — 엔진 보강 (8/15)

이슈 3건을 우리 선에서 처리했습니다. 셋 다 *"지금 돌리면 결과를 오독하게 되는"* 문제였습니다.

- [x] **#9 개체 기억** — 컨텍스트 누적 · MemGPT 압박 경고 · 축출 · `memory_write` · `MAX_STEPS` 폐지
- [x] **#8 산출물** — `raw_calls.jsonl` · 로그 4종 턴별 append · `reasoning` 이름 분리 · 실패 집계
- [x] **#7** — `intent` 폐지 · **모든 도구에 `reasoning` 필수** · 전역 `msg_id`
- [x] PR #12(동생의 #9 독립 구현)에서 개선 3건 병합 — 도구 토큰 회계 · `repeat_guard` 범위 · `step_cap`

실측으로 잡은 버그 — 잘린 JSON 에코로 400, 그 부작용인 **기억 삭제**, `run_id` 충돌로 인한
로그 오염. 셋 다 단위 테스트로는 안 나오고 실제 API 8턴을 돌려야 보였습니다.

```
8턴 실측   행동 0건 37% → 8%   ended 56 · API 오류 0(429 제외)
           프롬프트 …8932 에서 축출로 유지   memory_write 22회
비용       50턴 $0.66/런 · 20런 $13
```

- [x] **사전 v1** (PR #14, 동생) — 실제 변형 10건 누락 해소 · S2 층위 확충 · 오검출 수정.
      **v0 의 비대칭 버그를 잡았습니다** — `Asla·Ranoa·Miris` 가 fr 에만 있어 이름이
      양쪽에 다 있어도 "AI 가 주어를 생성했다" 로 잡혔습니다 (`ja→fr` 의 `sent` 가 0 이었음).
      subject 생성 43%→41% · 소실 21%→17% · tense 생성 32%→36%. 방향은 유지, 부풀림만 제거

## 지금 (8/16~18) — 과제 3: 채점과 지표

> 로그 스키마가 확정돼 추측 없이 쓸 수 있습니다. **우리가 진행합니다** (동생에게 통지함).

- [x] `tools/score/markers.py` — 화용 표지 카운터. 코퍼스 단위·방향별. 사전 커버리지 100%
- [x] `tools/score/judge.py` — 2단계 LLM 판정 + 언급률. `judged.jsonl` · `judge_raw.jsonl`
- [x] `tools/score/metrics.py` — 지표 전량 + `scored.jsonl` + 조건별 표
- [x] 런이 `knob_ai`·`seed` 를 스냅샷에 기록 (없으면 조건별 표를 만들 수 없었음)
- [x] `tools/score/xhat.py` — `x̂` 구간 추정 + `Δx`. **나이로 층화**
- [x] `events.jsonl` 에 `type=learn`, `state.jsonl` 에 `budget_start`
      (**`x̂` 의 전제였는데 빠져 있었습니다** — 지불액·할인 사유가 로그에 안 남았음)

**과제 3 완료.** 채점 도구 4종이 실제 런에서 끝까지 돕니다.

### 실측이 보여준 것 (6턴 노브48 · 8턴 노브24, 각 1시드)

```
할인 자기강화   t6 Ranoa2→ja 300원(정가) → t7 Ranoa3→ja 150원(할인)
                t7 Miris2→zh 300원(정가) → t8 Miris3→zh 150원(할인)
                ← spec 3.4 의 국가 단위 자기강화가 실제로 돈다
x̂              all L/2 ≤ x < L (n=19) · 3-5 x < L · 6+ L/2 ≤ x < L
표지            subject 소실 15~30% · tense 소실 44% · euphemistic_refusal 소실 100%(n=2)
                vs 수치 6a 소실 0~11%   ← "사실은 통과하고 의도만 깎인다"
전환            지표 3 = 0%(노브48) → 17%(노브24, n=23)
```

**Phase 0 재실행 때 확인할 것 3건** — 1시드 소턴이라 지금은 아무것도 결론내지 않습니다.

- [ ] **언급률의 선택 효과** — 4a 의 분모가 *언급된 메시지* 다. 6턴에서 경로별로
      달랐습니다 (AI 47% · 국내 35%). 차이가 유지되면 4a 비교를 그만큼 깎아 읽어야 합니다
- [ ] **`pair_dist` 편중** — 노브 48 에서 ja 발신 3건 vs zh 발신 21건이었는데
      노브 24 에서는 6방향이 다 나왔습니다. 노브 의존이면 4a 비교가 오염됩니다
- [x] ~~`hedge` 표본~~ — 43턴에서 n=18, 소실률 67%. 표본 문제 해소

## 끝난 것 — Phase 0 재실행 (8/16, 나흘 당김)

- [x] `tools/balance/verify_config.py` — 확정 config 를 창의 자로 재검증. **한 명령**
- [x] `w*` 재확인 — 무성장 0.45 / 성장 0.20. **차 0.00** (100턴 전환 뒤에도 그대로)
- [x] 창 [12960, 16200] · 임계 16038 (위치 0.95) · 벙커 3.00 주기분 · 1인부담 90 > 17.8
- [x] **12.7 절단 판단 — 하지 않습니다.** 일정이 닷새 앞서 있어 끌 이유가 없습니다
- [ ] 1런 소요시간·비용 → **Phase 1 런이 실측 중**

> ⚠️ **Phase 0 의 남은 한계.** `sweep.py` 의 정책 6종은 8/16 에 만든 배신 전략
> (벙커를 파면서 타국 출자를 받기)을 모형화하지 못합니다. `w*` 와 창은 구조적
> 요구량이라 영향이 없지만 **민감도 분석은 그만큼 낡았습니다.** RESULTS.md 에 적습니다.

## 끝난 것 — 세계 규칙 개정 (8/16)

프롬프트 전수 점검(`docs/prompt_audit.md`)에서 시작해 규칙이 여러 곳 바뀌었습니다.

- [x] **100턴 · 수명 2배** — 소통 왕복 하나에 두 턴. 기대수명 8턴이면 왕복 4회가 생애
      전부라 조율을 배울 시간이 구조적으로 없었습니다. 창은 선형이라 전부 정확히 2배
- [x] **생존 규칙을 프롬프트에** — 요격기가 뭘 하는지·벙커가 뭘 하는지가 **통째로
      없었습니다.** 43턴 실측의 "국토는 interceptor 인데 아무도 안 짓는다" 의 원인
- [x] **국토는 투표로만** — 제안 → 3턴 유예 → 採決. 침묵은 동의(1찬 0반도 통과).
      `propose_vote` 는 그전까지 **로그만 남기고 아무 일도 하지 않았습니다**
- [x] **낸 돈은 그 나라가 짓는 것에 들어간다 — 벙커라도.** 배신이 실제로 이득이 되고,
      *"너희 무엇을 짓고 있는가"* 가 가장 값비싼 정보가 됩니다
- [x] **타국 투자는 접수·과금만 답한다** — 안 그러면 10원짜리 국토 조회가 됩니다
- [x] **AI 번역을 고르면 원문은 못 본다** — 병기하면 학습자에게는 왜곡이 발생하지 않아
      4a 표본이 희석되고 노브 의존 가짜 효과가 생깁니다
- [x] 임계 공개 · 연도(42년부터) · 생산 배수 제거 · 유언을 `memory` 초기값으로
- [x] 부고(같은 나라만) · 진척 기여 통지 · `ask` 폐지 · `end_turn` 의 reasoning 제거
- [x] **로그 4종이 파일에 안 남고 있었습니다** — 투표·국토 전환·부고·진척 기여

## 끝난 것 — 뷰어 (8/16, 열흘 당김)

- [x] `viewer/index.html` — 의존성 없는 단일 HTML. GitHub Pages 그대로
- [x] **보낸 말과 도착한 말을 나란히** — 이 작품이 재는 그 차이를 눈에 보이게
- [x] 생존자 전원의 행동·근거·스텝·소요시간 · wellness 누적 · 열린 제안과 採決 연도
- [x] 시간 계측 — `elapsed_ms`/`llm_ms`/`ms_per_step` · `turn_wall_ms` · `elapsed_s`

```
python3 -m http.server
open http://localhost:8000/viewer/?run=phase1_knob48_seed1
```

- [ ] 데모 GIF

## 지금 (8/16) — Phase 1 (AI 없는 세계) **진행 중**

`comm_intl_ai = 48` 고정, 시드 1, 100턴. `runs/phase1_knob48_seed1`.
Phase 2 격자의 한 셀이라 런이 재사용됩니다.

- [ ] **① 학습이 일어나는가** ← 0 이면 Phase 2 가 무의미. 설계 재검토
      (`learning.py` ③: 학습률 0.1 아래면 단절 100%·회복 33%)
      ⚠ `learn_base` 를 600 으로 올린 뒤 **8턴 실측 0건**. 100턴에서 봐야 합니다
- [ ] **①' 투표가 열리는가** ← 새 관문. 아무도 제안 안 하면 100턴 내내 아무것도
      안 지어집니다 (8턴 실측에서는 t2·t3 에 제안 → t6·t7 採決 통과)
- [ ] ② 조율이 되는가   ③ 실제 기여율 `w` 실측
- [ ] **④ 배신이 나오는가** ← 벙커를 파면서 타국 출자를 받는가
- [ ] `memory_write` 를 실제로 쓰는가 ← 안 쓰면 인박스 3턴 유지 폴백
- [ ] 필요하면 임계 위치 재조정

## 8/23~26 — Phase 2 (본 실험)

- [ ] 노브 스윕 `[6, 12, 24, 48]` × 시드 (야간 배치)
- [ ] 중간 점검: 4a 흔들림 / 되묻기 0 / 원문직통 0 / 실패율 조건 간 차이
- [ ] `Δx = x̂(48) − x̂(6)`

## 8/28~29 — 문서

- [x] `README.md` — 팀명 + 연쇄 1줄 + 시좌 ①③ + 의도된 설계 2건 (8/13)
- [ ] `README.md` 심사용 갱신 — 본실험 결과 1줄 채우기
- [ ] `RESULTS.md` — 조건별 표, **실패도 정직하게**
- [ ] **주장 ↔ 증거 대응표**
- [ ] 사전 v1 의 남은 편향 한 줄 — 국가명을 3언어 모두에 넣어 **호격도 주어로 세므로**
      `sent`(소실률 분모)가 부풀어 소실률이 낮게 나옵니다. v0 의 비대칭보다는 낫습니다
- [ ] 발표자료
- [ ] 끈 항목을 "향후 방향"에

## 8/30

- [ ] 예비 + **제출 (23:59)**

---

## 일정 위험

**닷새쯤 앞섰습니다.** 엔진 8/15, 과제 3 8/16, Phase 0 8/16, 뷰어 8/16 —
계획보다 각각 사흘·나흘·나흘·열흘 빠릅니다. **12.7 절단은 하지 않습니다.**

다만 8/16 에 세계 규칙이 크게 바뀌었으므로 **Phase 1 이 진짜 관문입니다.**
학습·투표·조율·배신 넷 중 하나라도 0 이면 거기서 설계를 다시 봐야 합니다.

밀리면 **12.7 절단 순서**를 앞당겨 적용합니다.

```
0순위  화용 표지 자질 4개 → 2개      세계 규칙을 안 건드림. 측정만 축소
1순위  growth_coef = 0               시간 축 공공재를 끔
2순위  원문 직통 도박을 시스템 자동 결정
3순위  total_turns 50 → 30           계보 회전이 줄어 구전 감쇠가 안 보임
```

> **개체 기억(4.5)은 절단 후보가 아닙니다.** 끄면 조율이 기억 부재로 실패하고 그것을
> 번역 왜곡 탓으로 오독하게 됩니다. 줄인다면 `context_limit` 을 낮추는 것입니다.

**Phase 1 을 건너뛰지 마세요.** 거기서 학습이 0 이면 Phase 2 는 아무것도 못 보여줍니다.
차라리 Phase 2 의 시드를 줄이는 쪽이 낫습니다.
