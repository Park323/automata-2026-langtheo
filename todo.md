# TODO

```
팀명   カキガワ言語研究班
리포   github.com/Park323/automata-2026-langtheo
마감   8/30 23:59   ·   남은 15일 (8/15 기준)
```

> 팀명은 `README.md` 최상단, 발표자료 표지, 제출 폼에 동일하게.

## 끝난 것

- [x] 참가 등록 + Discord · 파일럿 문장 10개 · 화용 표지 사전 v0
- [x] **번역 파일럿** (8/12) — 324콜. 모델 `mistral-small-3.2-24b` 확정,
      길이비 `fr:ja:zh = 100:33:23` 실측, 지표 6a 정의 수정, 숫자 0/34 소실
- [x] **Phase 0 사전 밸런스** (8/12~13) — 설계 결함 8건을 세계 만들기 전에 적발.
      판정 기준을 회피율 → `w*` 로 교체
- [x] **세계 구조 전환** (8/13) — 세대 경계 폐지, 확률적 수명, 행복→수명,
      `procreate`, 학습 할인, 파라미터 전량 확정
- [x] **과제 1** (PR #4) — config 로더 + assert 8개 + 턴 루프 7단계 (LLM 없이)
- [x] **과제 2** (PR #6) — function calling agentic loop · 메시지 · 번역 · 3턴 스모크
- [x] 프롬프트 3언어화 + 언어 위생 (8/15) — 에이전트가 영어로 쓰던 문제

## 끝난 것 — 엔진 보강 (8/15)

이슈 3건을 우리 선에서 처리했습니다. 셋 다 *"지금 돌리면 결과를 오독하게 되는"* 문제였습니다.

- [x] **#9 개체 기억** — 컨텍스트 누적 · MemGPT 압박 경고 · 축출 · `memory_write` · `MAX_STEPS` 폐지
- [x] **#8 산출물** — `raw_calls.jsonl` · 로그 4종 턴별 append · `reasoning` 이름 분리 · 실패 집계
- [x] **#7** — `intent` 폐지 · **모든 도구에 `reasoning` 필수** · 전역 `msg_id`
- [x] PR #12(동생의 #9 독립 구현)에서 개선 3건 병합 — 도구 토큰 회계 · `repeat_guard` 범위 · `step_cap`

실측으로 잡은 버그 — 잘린 JSON 에코로 400, 그 부작용인 **기억 삭제**, `run_id` 충돌로 인한
로그 오염. 셋 다 단위 테스트로는 안 나오고 실제 API 8턴을 돌려야 보였습니다.

```
8턴 실측   행동 0건 37% → 8%   ended 56 · API 오류 0(429 제외)
           프롬프트 …8932 에서 축출로 유지   memory_write 22회
비용       50턴 $0.66/런 · 20런 $13
```

- [ ] 사전 표지 목록 보강 (표본 10문장은 얇음. S2 층위를 못 잡음)

## 지금 (8/16~18) — 과제 3: 채점과 지표

> 로그 스키마가 확정돼 추측 없이 쓸 수 있습니다. **우리가 진행합니다** (동생에게 통지함).

- [x] `tools/score/markers.py` — 화용 표지 카운터. 코퍼스 단위·방향별. 사전 커버리지 100%
- [x] `tools/score/judge.py` — 2단계 LLM 판정 + 언급률. `judged.jsonl` · `judge_raw.jsonl`
- [x] `tools/score/metrics.py` — 지표 전량 + `scored.jsonl` + 조건별 표
- [x] 런이 `knob_ai`·`seed` 를 스냅샷에 기록 (없으면 조건별 표를 만들 수 없었음)
- [ ] **`x̂` 추출** — 학습이 켜지고 꺼지는 임계. 눈금 `L/L2/L4`, **나이로 층화**
      (층화 입력은 `metrics.learner_rate(...)['by_age']` 로 이미 나옵니다)

### 채점기가 6턴 실측에서 잡은 것

```
학습    t3 Asla1 → t4 Asla2 → t5 Asla3·Miris2 → t6 Asla3 이 fr 추가
        learn_base 300 · income 100/턴 인데 실제로 일어난다  ← Phase 1 ①번 관문 통과 신호
표지    subject 소실 30%·생성 18% · tense 소실 44%   vs   수치 6a 소실 0%
        ← "사실은 통과하고 의도만 깎인다" 가 본 세계에서도 재현
전환    지표 3 = 0% (n=14). 국제 메시지가 propose_vote 로 이어지지 않았다
방향    fr→zh 48% · zh→fr 39% · ja→zh 9% · zh→ja 4%   ← ja 발신이 얇다
```

**Phase 0 재실행 때 확인할 것 3건** — 6턴·1시드라 지금은 아무것도 결론내지 않습니다.

- [ ] **언급률의 선택 효과** — 4a 의 분모가 *언급된 메시지* 다. 6턴에서 경로별로
      달랐습니다 (AI 47% · 국내 35%). 차이가 유지되면 4a 비교를 그만큼 깎아 읽어야 합니다
- [ ] **`pair_dist` 편중** — ja 발신 3건 vs zh 발신 21건. 유지되면 4a 비교가 오염됩니다
- [ ] **`hedge` 표본** — 6턴에서 3건뿐. 유보가 거의 없으면 "유보가 깎인다" 를 관측할 수 없습니다

## 8/20 — Phase 0 재실행 + 캘리브레이션

- [ ] 1런 소요시간·비용 실측 (누적 컨텍스트라 프롬프트가 커짐. 캐싱 적중률 확인)
- [ ] `w*` 재확인 — 무성장 0.45 / 성장 0.20
- [ ] 벙커 깊이 배수 · `growth_coef` · `growth_scale`
- [ ] **⚠️ 12.7 1순위 절단 판단 — 이 시점이 마지막**

## 8/21~22 — Phase 1 (AI 없는 세계)

`comm_intl_ai = 48` 고정. Phase 2 격자의 한 셀이라 런이 재사용됩니다.

- [ ] **① 학습이 일어나는가** ← 0 이면 Phase 2 가 무의미. 설계 재검토
      (`learning.py` ③: 학습률 0.1 아래면 단절 100%·회복 33%)
- [ ] ② 조율이 되는가   ③ 실제 기여율 `w` 실측
- [ ] `memory_write` 를 실제로 쓰는가 ← 안 쓰면 인박스 3턴 유지 폴백
- [ ] 필요하면 임계 위치 재조정

## 8/23~26 — Phase 2 (본 실험)

- [ ] 노브 스윕 `[6, 12, 24, 48]` × 시드 (야간 배치)
- [ ] 중간 점검: 4a 흔들림 / 되묻기 0 / 원문직통 0 / 실패율 조건 간 차이
- [ ] `Δx = x̂(48) − x̂(6)`

## 8/27~28 — 뷰어

- [ ] GitHub Pages 제로 설치 뷰어
- [ ] 데모 GIF

## 8/28~29 — 문서

- [x] `README.md` — 팀명 + 연쇄 1줄 + 시좌 ①③ + 의도된 설계 2건 (8/13)
- [ ] `README.md` 심사용 갱신 — 본실험 결과 1줄 채우기
- [ ] `RESULTS.md` — 조건별 표, **실패도 정직하게**
- [ ] **주장 ↔ 증거 대응표**
- [ ] 발표자료
- [ ] 끈 항목을 "향후 방향"에

## 8/30

- [ ] 예비 + **제출 (23:59)**

---

## 일정 위험

**엔진이 사흘쯤 앞섰습니다.** 8/17~19 예정이던 엔진이 8/15 에 끝나 과제 3 이 당겨졌습니다.
8/20 절단 판단까지 여유가 생겼지만, **Phase 1 이 병목입니다.**

밀리면 **12.7 절단 순서**를 앞당겨 적용합니다.

```
0순위  화용 표지 자질 4개 → 2개      세계 규칙을 안 건드림. 측정만 축소
1순위  growth_coef = 0               시간 축 공공재를 끔
2순위  원문 직통 도박을 시스템 자동 결정
3순위  total_turns 50 → 30           계보 회전이 줄어 구전 감쇠가 안 보임
```

> **개체 기억(4.5)은 절단 후보가 아닙니다.** 끄면 조율이 기억 부재로 실패하고 그것을
> 번역 왜곡 탓으로 오독하게 됩니다. 줄인다면 `context_limit` 을 낮추는 것입니다.

**Phase 1 을 건너뛰지 마세요.** 거기서 학습이 0 이면 Phase 2 는 아무것도 못 보여줍니다.
차라리 Phase 2 의 시드를 줄이는 쪽이 낫습니다.
