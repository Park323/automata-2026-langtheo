# RESULTS

> カキガワ言語研究班 — AUTOMATA HACKATHON Vol.2
> 본실험 결과는 아래 "본실험"에 추가됩니다. 현재는 파일럿과 Phase 0 까지입니다.

---

## 파일럿 — 번역 경로 검증 (2026-08-12)

`docs/spec.md` 12.2. **엔진을 만들기 전에** 번역 경로가 성립하는지 확인한 것입니다.
번역에서 화용 자질이 거의 온전히 보존되면 실험 자체가 성립하지 않으므로,
세계를 만들기 전에 먼저 봤습니다.

### 설정

```
문장      docs/pilot/sentences.json  10개 × ja/zh/fr
방향      6개 (ja→zh ja→fr zh→ja zh→fr fr→ja fr→zh)
지시      3종 (무지시 / 정확히 / 간결히) — S1·S2·S6·S10 에만
모델      mistral-small-3.2-24b · gemma-3-27b · llama-3.3-70b  (OpenRouter)
호출      324회, 성공 295 / 레이트리밋 실패 29
채점      domains/meteor/lexicon/markers.yaml 로 규칙 기반. LLM 판정 없음
원자료    runs/pilot/20260812_170123/raw.jsonl
```

### ① 수치 소실·변조 — 0/34

**지표 6a가 대조군으로 성립합니다.**

| 모델 | 소실·변조 | 추가 |
|---|---|---|
| gemma-3-27b | **0**/12 | 1/12 |
| llama-3.3-70b | **0**/12 | 2/12 |
| mistral-small-24b | **0**/10 | 2/10 |

추가 5건은 전부 표기 변환이었습니다.

```
zh 「另外两国」(단어 수사) → ja 「他の2か国」(아라비아 숫자)
   원문 [30] → 출력 [2, 30].  30 은 완벽 보존
```

> **이 때문에 지표 6a의 정의를 집합 일치에서 부분집합으로 고쳤습니다.**
> 집합 일치로 재면 표기 변환이 "왜곡"으로 잡혀 대조군이 오염됩니다.

### ② 3언어 균형 — 모델 선택 기준

방향 간 손실률의 **분산**이 작을수록 좋습니다. 한 언어만 잘하면 노브와 무관한
국가 비대칭이 생기기 때문입니다.

| 모델 | 평균 소실률 | **표준편차** |
|---|---|---|
| **mistral-small-24b** | 13.2% | **11.5%** ← 최소 |
| gemma-3-27b | 12.4% | 12.6% |
| llama-3.3-70b | 22.0% | 17.2% |

**mistral 1위, gemma 근소한 2위.** 다만 mistral 표본이 레이트리밋으로 적어(10 vs 12)
차이가 확정적이지 않습니다.

> 선택 기준은 **사전에 고정**했습니다 — 숫자 보존 / 3언어 균형 / 값싼 등급 /
> 에이전트와 다른 계열. **왜곡 정도는 기준에 넣지 않았습니다.**
> 왜곡이 나올 때까지 모델을 바꾸면 결과를 만들어내는 것이 됩니다.

### ③ 자질별 소실·생성 — 예측이 하나 빗나갔습니다

| 자질 | 예측 | 실측 |
|---|---|---|
| `condition` | 깎여서 조건부 동의가 확약이 된다 | **소실률 ≈ 0%. 잘 보존됨** |
| `hedge` | — | **소실률 17~100%. 이쪽이 깎임** |
| `subject` (`ja→fr`) | 주어가 생성된다 | **1.6~1.7건/문장. 적중** |
| `subject` (`fr→ja`) | — | 소실률 44~69% |

**조건절은 살아남고 헤지가 죽습니다.** 연쇄의 초점을 조정해야 할 수 있습니다.

주어 생성은 예측대로였습니다. `→fr` 방향에서만 일어나고 `→zh`·`→ja` 에서는
일어나지 않습니다 — **언어쌍이 곧 대조군**이라는 주장이 실측으로 확인됐습니다.

### ④ 사전의 한계 — 표지는 남고 기능이 뒤집힘

S2(완곡 거절)에서 드러났습니다.

```
ja 「検討させていただきます」   ← 사실상 거절
 → zh 「我会考虑一下」
 → fr 「Je vais examiner cela」  ← 진짜 검토로 읽힘
```

**표지는 보존됐습니다**(`検討` → `考虑一下` / `examiner`). 사전은 소실을 잡지 못합니다.
그런데 **화용적 기능은 뒤집혔습니다.**

> 사전은 *"표지가 있는가"*만 세고 *"같은 기능을 하는가"*는 세지 못합니다.
> 여기가 **LLM 이진 판정(의도 전달 성공)이 필요한 층위**입니다.
> 두 채점 방식의 역할 분담이 이 관측으로 명확해졌습니다.

### ⑤ 번역 지시는 유효합니다

| 모델 | 무지시 | 정확히 | 간결히 |
|---|---|---|---|
| gemma-3-27b | 1.4% | 0.0% | **7.1%** |
| mistral-small-24b | 0.0% | 0.0% | **6.4%** |
| llama-3.3-70b | 12.9% | 8.6% | 8.6% |

**"간결하게"가 손실을 만듭니다.** `translate_instruction`을 발신 에이전트에게 맡긴
설계(5.2)를 유지합니다.

### ⑥ 언어별 길이 상한 — 실측

같은 내용에 필요한 글자 수입니다.

| | ja | zh | fr |
|---|---:|---:|---:|
| 원문 10문장 합계 (손번역) | 256 | 163 | 636 |
| **번역 출력 평균 (실측)** | 25~28 | 17~19 | **72~86** |
| **비율 (fr = 100)** | **29~37** | **21~25** | **100** |

`fr`은 `ja`의 약 3배, `zh`의 약 4배 글자를 씁니다.

> **균일 상한을 걸었다면 `fr`이 구조적으로 불리해졌을 것입니다.**
> 결과가 나온 뒤 *"C국이 조율에 실패한 것이 번역 탓인가 글자 수 탓인가"*를
> 구분할 수 없었을 것입니다.

`fr` 상한을 먼저 정하고 비례 배분합니다. 예: `fr 200 → ja 66 / zh 46`.

### ⑦ 번역 확신도 — 디커플링 조짐

| 모델 | logprob 평균 |
|---|---|
| gemma-3-27b | **-0.036** ← 가장 확신에 참 |
| llama-3.3-70b | -0.103 |
| mistral-small-24b | -0.176 |

가장 확신에 찬 gemma가 헤지 소실은 중간입니다.
**유창함과 정확함이 어긋난다**는 초기 증거이며, 본실험에서 메시지 단위로 상관을 봅니다.

`logprobs`는 모델이 아니라 **프로바이더**에 달렸습니다. OpenRouter가 같은 모델을
여러 곳으로 라우팅하므로 `provider: {"require_parameters": true}` 로 강제해야 하고,
**Qwen 계열은 강제해도 반환하지 않습니다.**

### 한계

- **표본이 작습니다.** 문장 10개 × 6방향에 오류 29건이라 자질별 셀당 몇 건뿐입니다.
  방향성은 믿을 만하지만 **수치는 확정이 아닙니다**
- 원문 3언어는 **저자 1인의 번역**입니다. 길이 비율은 다른 판본과 대조해야 합니다
- 레이트리밋 실패가 모델별로 달라(mistral 10 vs gemma 12) 모델 간 비교가
  완전히 대등하지 않습니다

### 이 파일럿이 정한 것

```
번역 모델   mistral-small-3.2-24b   3언어 분산 최소
에이전트    Qwen 계열               번역기와 다른 계열이 자동 보장
백엔드      OpenRouter API          본실험 전체 $4~5. 로컬 불필요
길이 상한   fr 기준 비례 배분 → fr 400 / ja 130 / zh 90
지표 6a     집합 일치 → 부분집합 검사
```

확정값 전체는 아래 "확정된 값" 에 모아뒀습니다.

---

## Phase 0 — 사전 밸런스 (2026-08-12 ~ 08-13)

`tools/balance/sweep.py` · `tools/balance/learning.py`.
**세계를 만들기 전에** LLM 없이 파라미터 공간을 훑은 것입니다. LLM 에이전트 대신
규칙 기반 정책을 쓰고, 실제 에이전트는 그 공간 어딘가에 떨어진다고 가정했습니다.

> 이것은 세계의 **경제 구조**를 맞추는 도구이지 가설을 검증하는 도구가 아닙니다.
> 번역 왜곡의 효과는 `coord`(조율 성공률) 한 숫자로 추상화되어 있을 뿐 모형화되지
> 않았습니다.

### 잡아낸 설계 결함 — 8건

**전부 세계를 만들기 전에 나왔습니다.** 코드를 쓴 뒤에 발견했다면 이미 돌린 런이
전부 무효였을 것들입니다.

| # | 결함 | 증상 |
|---|---|---|
| 1 | **단위 오류** | 임계값은 진척 단위인데 assert 는 소득 단위로 비교. `p=0.3` 이면 요구량이 3배 부풀려짐 |
| 2 | **부등식 누락 2개** | ★B(조율 강제)와 ★C(도달 가능)가 없어, 한 나라가 혼자 해내거나 아무도 도달 못 하는 조합이 통과 |
| 3 | **스펙 모순** | 2.3은 *"요격기는 3국이 내야"*, 4.4는 *"자국 국토에만 투자"* — 유치국 혼자 내게 되어 **구조적으로 도달 불가능** |
| 4 | **지표 2 정의 부재** | *"run별 생존 판정"* 이 전 인류인지 일부인지 없음. 일부 생존을 성공으로 세면 각자 벙커만 파도 성공이라 **조율이 무의미**해짐 |
| 5 | **벙커가 확정 생존** | 확실한 차선이 있으면 조율할 이유가 없음. 깊이 확률로 교체 |
| 6 | **`initial_budget` 이 돈을 찍어냄** | 출생마다 지급되므로 **사망이 화폐 발행**. 수명이 짧을수록 나라가 부유해지고 `wellness`(수명 연장)가 국가 소득을 깎는 역설 |
| 7 | **`growth_scale` 이 성장 축을 사문화** | 2000 은 전 기간 총소득보다 커서 배수가 1.15 까지만 오름. `growth_coef` 를 뭘로 두든 결과가 같았음 |
| 8 | **임계 유도와 시뮬레이션의 가정 불일치** | 임계는 "전액이 다음 세대 자본" 을 가정해 유도되는데 시뮬은 국가 투자를 해야 성장. 성장을 전제로 임계를 잡으면 *"국가 투자를 안 하면 구조적으로 도달 불가"* 가 됨 |

**6번의 조치가 특히 깔끔했습니다.** `initial_budget = 0` 으로 두니 역설이 사라지고,
동시에 `procreate`(예산 상속)가 실질적 가치를 갖게 됐습니다 — 태어날 때 공짜로 50을
받으면 물려받을 이유가 약합니다.

### 판정 기준을 두 번 바꿨습니다

```
1차   재앙 회피율이 40~60% 에 오는가
2차   조율률을 낮췄을 때 회피율이 충분히 갈리는가 (민감도)
3차   필요 기여율 w* 가 목표(0.5)에 오는가          ← 현재
```

**회피율은 목표로 삼을 수 없는 양이었습니다.** 50턴 × 턴당 수십 번의 베르누이 시행이라
대수의 법칙으로 진척이 사실상 결정적이 되고, 그래서 **회피율이 조율 성공률을 그대로
복사**합니다. 임계값을 창 안에서 끝까지 밀어도 정책별 승률이 `0.00` 아니면 `0.92` 로만
나왔습니다 (조율률 0.9 에서 0.92).

```
      임계    자기중심  벙커  이타  헤저  무임승차  원시안   평균
pos 0.00  1210   0.00  0.00  0.92  0.95   0.00   0.00   0.31
pos 0.50  1361   0.00  0.00  0.92  0.02   0.00   0.00   0.16
pos 1.00  1512   0.00  0.00  0.92  0.02   0.00   0.00   0.16
```

대신 정책에 연속 축(소득 중 타국 요격기에 내는 비율 `w`)을 넣으니 손잡이가 생겼습니다.
**`w*` = 조율이 완벽할 때 살아남는 데 필요한 최소 기여율.** 임계 위치를 따라 연속적으로
움직입니다.

### 세계를 바꾸고 재실행했습니다

08-13 에 세대 경계를 폐지하고 확률적 수명으로 전환했습니다 (`docs/spec.md` 2.2).
도구도 턴 루프 + 개인별 사망/출생으로 재작성했습니다.

```
세대 경계 없음 · 50턴 · 기대수명 8.3턴
후보 90개 / assert 통과 90개

    p   초기예산  벙커깊이  임계위치  사망수    w*   민감도  저조율  고조율
  0.3      0     3.0    0.95   39.7  0.45   0.19   0.12   0.32

창 : A 2700  B 4500  E 6480  <  임계 8019  <  C×0.6 8100
현실 투자율에서 필요한 주기 수 : 4.5 / 5
벙커 : 한 주기 전력 → 생존 28%,  전 기간 → 81%

정책별 (조율률 0.9)
  이타 0.92 · 헤저 0.92 · 원시안 0.92 · 벙커 0.08 · 장수 0.00 · 무임승차 0.00
```

**`w*` 는 0.45 가 최대입니다.** 목표 0.5 에 못 미치며, 임계를 창 끝(0.95)까지 밀어야
나오는 값이라 더 올릴 수 없습니다. *"소득의 45%를 국제 협력에 내야 산다"* 로 받거나,
창을 넓히려면 정책계수 0.6 을 올려야 합니다.

**★E(지속 참여)를 새로 넣었습니다.** 한 주기가 통째로 쉬면 도달 불가. 전력 기준으로
걸면 하한이 `(G−1)/G × C` 가 되어 상한을 넘고 창이 닫히므로 양변에 같은 정책계수를
씁니다. 걸어본 결과 **민감도를 깎지 않았습니다** — 다만 이것은 규칙 기반 정책이 공간의
꼭짓점에 있어 임계 이동에 둔감한 탓이고, 실제 LLM 에이전트는 그 사이에 떨어질 테니
난이도는 확실히 올라갑니다.

### 이전 수치는 신뢰할 수 없었습니다

시드가 `random.Random(hash((name, cd)))` 였는데 **문자열 `hash()` 는 프로세스마다
소금이 달라** 실행할 때마다 1위 조합이 바뀌고 있었습니다. 같은 명령을 두 번 돌렸을 때
`p=0.7` 과 `p=0.5` 가 번갈아 1위로 나온 것이 그 때문입니다.

문자열을 그대로 시드로 주도록 고쳐 재현성을 확보했고(두 번 실행 동일 출력 확인),
**그 이전에 기록한 민감도 수치들은 ±0.03~0.05 흔들림이 있었다고 보아야 합니다.**
결함 발견이나 벙커 확률화 같은 큰 결론은 그대로지만, 조합 간 근소한 순위 차는
근거가 되지 못했습니다.

---

## Phase 0 — 학습 눈금과 단절 임계 (2026-08-13)

`tools/balance/learning.py`. 몬테카를로 없이 산수로 **`x` 를 읽을 자가 제자리에 있는지**
확인합니다. ③만 예외로 시뮬레이션입니다.

### 눈금 셋

할인 구조(`docs/spec.md` 3.4)가 조건을 늘리지 않고 눈금을 셋으로 만들어 줍니다.

| 눈금 | 비용 | 저축턴 | 생애 비중 | 판정 |
|---|---:|---:|---:|---|
| `L` (연고 없음) | 300 | 3.0 | 48% | 쓸 수 있는 눈금 |
| `L/2` (국내 구사자 **또는** 부모) | 150 | 1.5 | 36% | 쓸 수 있는 눈금 |
| `L/4` (국내 구사자 **와** 부모) | 75 | 0.8 | 24% | 자의 바닥 — 예산이 제약을 멈춤 |

`x` 측정 상한은 `income × (기대수명 − ap.learn) = 728` 이고, 세 눈금이 그 안에
들어갑니다. **`x` 를 `[0,75) [75,150) [150,300) [300,∞)` 네 구간으로 좁힙니다.**

### 나이가 자를 흔듭니다 — 층화가 필수입니다

세대가 없어져 "남은 생애" 가 개인마다 다릅니다.

| 나이 | 기대 잔여 | 남은 소득 | 감당 가능한 눈금 |
|---:|---:|---:|---|
| 0 | 8.28 | 828 | `L` · `L/2` · `L/4` |
| 4 | 4.29 | 429 | `L` · `L/2` · `L/4` |
| 6 | 2.49 | 249 | `L/2` · `L/4` |
| 8 | 1.32 | 132 | `L/4` 만 |

**이것이 `x` 추정의 최대 노이즈원입니다.** 학습 여부만 보면 *"x 가 작아서"* 인지
*"늙어서"* 인지 구분되지 않습니다. `events.jsonl` 에 나이를 함께 남겨 층화해야 합니다.

### 마지막 구사자가 죽으면 되돌릴 수 없습니다

국내 구사자가 0 이 되면 비용이 2배가 되고, 그래서 더 안 배우게 됩니다.
학습률 `p`(턴당 국가별 신규 학습자 기대수)별로 400회 시뮬했습니다.

| 학습률 `p` | 단절 발생 | 첫 단절 | 단절 후 회복 |
|---:|---:|---:|---:|
| 0.02 | 100% | 8턴 | 6% |
| 0.05 | 100% | 9턴 | 18% |
| 0.10 | 100% | 11턴 | 33% |
| 0.20 | 94% | 16턴 | 60% |
| 0.40 | 66% | 23턴 | 83% |

**학습률이 0.1 아래면 단절이 사실상 확실하고, 한번 끊기면 거의 회복하지 못합니다.**
이것이 연쇄 2칸(학습자 감소)의 **자기강화 메커니즘**이며, 노브와 무관하게도 일어날 수
있습니다. 따라서 **Phase 1 에서 학습률이 0.1 을 넘는지가 관측 가능성의 실질 기준**
입니다 — 넘지 못하면 노브를 내려 학습이 줄어드는 것을 관측할 여지 자체가 없습니다.

---

## 확정된 값

```
번역 모델   mistral-small-3.2-24b     3언어 분산 최소 (파일럿 ②)
에이전트    Qwen 계열                 번역기와 다른 계열이 자동 보장
백엔드      OpenRouter API            본실험 전체 $4~5

income 100 (기준 단위) · initial_budget 0 · success_prob 0.3
comm 3 / 5 · 노브 [6, 12, 24, 48] · learn_base 300 · ask 3 · propose_vote 6
survival k 8 / λ 8.26 (기대수명 8.3턴) · wellness.gain 0.008 · growth 0.3 / 1500
total_turns 50 · epoch_turns 10 · interceptor 8019 · bunker_scale 2700
길이 fr 400 / ja 130 / zh 90 · 지표 6a 부분집합 검사
```

## 한계

- `coord` 는 번역 왜곡의 **추상화**입니다. 실제 왜곡이 조율률을 얼마나 떨어뜨리는지는
  본실험에서만 알 수 있습니다
- 규칙 기반 정책은 공간의 **꼭짓점**이고, LLM 에이전트가 그 안에 떨어진다는 보장이
  없습니다. 그래서 임계 이동에 둔감하게 나오는 부분이 있습니다
- 소통 비용·AP·학습은 `sweep.py` 에 모형화되어 있지 않습니다. 요격기 경제만 맞춘 것입니다
- `learning.py` ③ 의 학습률 `p` 는 외생 입력입니다. 실제 학습률은 Phase 1 에서만 나옵니다
- 파일럿의 한계는 위 파일럿 절에 따로 적었습니다 (표본 크기, 1인 번역, 레이트리밋)

---

## 본실험

아직입니다. Phase 1(AI 없는 세계) → Phase 2(노브 스윕) 순으로 진행하며,
조건별 표와 **주장 ↔ 증거 대응표**로 기록합니다. 실패도 정직하게 남깁니다.
