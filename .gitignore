# Claude Code (machine-local settings)
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Environment — 절대 커밋하지 않는다
.env
.env.*
!.env.example

# OS
.DS_Store

# Simulation outputs
outputs/
results/

# 손으로 파본 흔적 — raw_calls 조각 같은 것. 저장소에 남을 것이 아니다
memo
memo/
scratch/
*.scratch
logs/
# 산출물은 제외하되 **견본 두 판은 동봉한다** (8/29 · 심사 대비) — 리포만 받아도
# 뷰어가 돌아야 한다. raw_calls(수십 MB)와 복원점은 뺀다: 뷰어가 안 읽는다.
runs/*
!runs/pilot/
!runs/260827-009-ai006/
runs/260827-009-ai006/raw_calls.jsonl
runs/260827-009-ai006/checkpoints/
!runs/260827-001-risk-noai/
runs/260827-001-risk-noai/raw_calls.jsonl
runs/260827-001-risk-noai/checkpoints/

# 8/21 에 1.1MB 짜리 Archive.zip 이 실수로 커밋에 실려 스택 전체를 타고 왔다 (2f49bfd).
*.zip
